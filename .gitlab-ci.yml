stages:
  - secrets
  - cache
  - test
  - build

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  DOCKER_TLS_CERTDIR: "/certs"
  DOCKER_IMAGE_TAG: "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA"

gitleaks:
  stage: secrets
  image:
    name: zricethezav/gitleaks:latest
    entrypoint: [""]
  script:
    - mkdir -p reports
    - gitleaks detect --source . --verbose --redact --report-format json --report-path reports/gitleaks.json
  artifacts:
    when: always
    paths:
      - reports/gitleaks.json

warm-cache:
  stage: cache
  image: python:3.12-slim
  cache:
    key:
      files:
        - pyproject.toml
    paths:
      - .cache/pip
    policy: pull-push
  script:
    - |
      if [ -d .cache/pip/wheels ] && [ "$(find .cache/pip/wheels -type f | wc -l)" -gt 0 ]; then
        echo "Python dependency cache already exists for this pyproject.toml key."
      else
        echo "Python dependency cache missing. Downloading wheels."
        python -m pip install --upgrade pip
        python -m pip download ".[dev]" --dest .cache/pip/wheels
      fi

bandit:
  stage: test
  image: python:3.12-slim
  cache:
    key:
      files:
        - pyproject.toml
    paths:
      - .cache/pip
    policy: pull
  before_script:
    - python -m pip install --upgrade pip
    - python -m pip install --find-links .cache/pip/wheels ".[dev]"
  script:
    - mkdir -p reports
    - python -m bandit -r app -f json -o reports/bandit.json
  artifacts:
    when: always
    paths:
      - reports/bandit.json

semgrep:
  stage: test
  image: python:3.12-slim
  cache:
    key:
      files:
        - pyproject.toml
    paths:
      - .cache/pip
    policy: pull
  before_script:
    - python -m pip install --upgrade pip
    - python -m pip install --find-links .cache/pip/wheels ".[dev]"
  script:
    - mkdir -p reports
    - semgrep scan --config auto --error --json-output reports/semgrep.json --gitlab-sast-output reports/semgrep-gitlab-sast.json app
  artifacts:
    when: always
    paths:
      - reports/semgrep.json
      - reports/semgrep-gitlab-sast.json
    reports:
      sast: reports/semgrep-gitlab-sast.json

test:
  stage: test
  image: python:3.12-slim
  cache:
    key:
      files:
        - pyproject.toml
    paths:
      - .cache/pip
    policy: pull
  before_script:
    - python -m pip install --upgrade pip
    - python -m pip install --find-links .cache/pip/wheels ".[dev]"
  script:
    - python -m pytest

lint:
  stage: test
  image: python:3.12-slim
  cache:
    key:
      files:
        - pyproject.toml
    paths:
      - .cache/pip
    policy: pull
  before_script:
    - python -m pip install --upgrade pip
    - python -m pip install --find-links .cache/pip/wheels ".[dev]"
  script:
    - python -m ruff check .

build:
  stage: build
  image: docker:27
  services:
    - docker:27-dind
  needs:
    - bandit
    - semgrep
    - test
    - lint
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - docker build -t "$DOCKER_IMAGE_TAG" .
    - docker push "$DOCKER_IMAGE_TAG"
    - |
      if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then
        docker tag "$DOCKER_IMAGE_TAG" "$CI_REGISTRY_IMAGE:latest"
        docker push "$CI_REGISTRY_IMAGE:latest"
      fi
