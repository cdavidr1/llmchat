apiVersion: v1
kind: Service
metadata:
  name: python-vault-app
  namespace: vault-demo
spec:
  selector:
    app: python-vault-app
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
